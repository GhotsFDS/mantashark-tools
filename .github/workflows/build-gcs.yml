name: Build MantaShark GCS

on:
  push:
    branches: [main]
    paths:
      - 'tools/manta_gcs/**'
      - '.github/workflows/build-tuner.yml'
  workflow_dispatch:
  release:
    types: [created]

permissions:
  contents: write   # 允许 softprops/action-gh-release 写 release assets

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - uses: actions/setup-node@v5
        with:
          node-version: '20'

      - name: Install Python deps
        run: |
          python -m pip install --upgrade pip
          pip install pymavlink "websockets<13" pyserial pyinstaller

      - name: Build Tuner HTML (single-file React)
        run: |
          cd tools/manta_gcs
          npm install
          npm run build

      - name: Build mavbridge.exe
        run: |
          cd tools/manta_gcs
          pyinstaller --clean --noconfirm mavbridge.spec
          if (-not (Test-Path "dist/mavbridge.exe")) { throw "mavbridge.exe missing" }
          $size = (Get-Item "dist/mavbridge.exe").Length / 1MB
          Write-Host ("mavbridge.exe: {0:N1} MB" -f $size)

      - name: Smoke test mavbridge.exe (--help)
        run: |
          cd tools/manta_gcs
          dist/mavbridge.exe --help

      - name: Assemble release bundle
        run: |
          cd tools/manta_gcs
          New-Item -Force -ItemType Directory release/MantaShark-GCS-windows | Out-Null
          Copy-Item dist/mavbridge.exe release/MantaShark-GCS-windows/
          Copy-Item dist/index.html    release/MantaShark-GCS-windows/GCS.html
          @'
          @echo off
          chcp 65001 >nul 2>&1
          title MantaShark 地面站
          cd /d "%~dp0"
          echo [launcher] 浏览器打开 GCS.html
          start "" "%CD%\GCS.html"
          echo.
          echo ===== 启动 mavbridge.exe - 选串口和波特率 =====
          mavbridge.exe
          echo.
          pause
          '@ | Out-File -Encoding ascii release/MantaShark-GCS-windows/start.bat
          @'
          MantaShark 地面站 v9 - Windows release

          1. Double-click start.bat
          2. Choose COM port and baudrate in the terminal
          3. The Tuner UI auto-opens in your default browser

          Files:
            start.bat       launcher
            mavbridge.exe   MAVLink<->WebSocket bridge (Python runtime bundled)
            GCS.html      single-file React Tuner

          No Python or Node needed. Unzip and run.
          '@ | Out-File -Encoding utf8 release/MantaShark-GCS-windows/README.txt
          Compress-Archive -Path release/MantaShark-GCS-windows/* -DestinationPath release/MantaShark-GCS-windows.zip -Force

      - uses: actions/upload-artifact@v7
        with:
          name: MantaShark-GCS-windows
          path: tools/manta_gcs/release/MantaShark-GCS-windows.zip
          retention-days: 30

      - name: Attach to release
        if: github.event_name == 'release'
        uses: softprops/action-gh-release@v2
        with:
          files: tools/manta_gcs/release/MantaShark-GCS-windows.zip

  build-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - uses: actions/setup-node@v5
        with:
          node-version: '20'

      - name: Install Python deps
        run: |
          python -m pip install --upgrade pip
          pip install pymavlink "websockets<13" pyserial pyinstaller

      - name: Build Tuner HTML
        run: |
          cd tools/manta_gcs
          npm install
          npm run build

      - name: Build mavbridge (Linux)
        run: |
          cd tools/manta_gcs
          pyinstaller --clean --noconfirm mavbridge.spec
          test -f dist/mavbridge

      - name: Assemble Linux bundle
        run: |
          cd tools/manta_gcs
          mkdir -p release/MantaShark-GCS-linux
          cp dist/mavbridge       release/MantaShark-GCS-linux/
          cp dist/index.html      release/MantaShark-GCS-linux/GCS.html
          cat > release/MantaShark-GCS-linux/start.sh <<'EOF'
          #!/usr/bin/env bash
          cd "$(dirname "$(readlink -f "$0")")"
          URL="file://$(pwd)/GCS.html"
          echo "[launcher] 打开 $URL"
          (xdg-open "$URL" >/dev/null 2>&1 &) || (firefox "$URL" >/dev/null 2>&1 &) || echo "手动打开 $URL"
          echo
          echo "===== 启动 mavbridge - 选串口和波特率 ====="
          ./mavbridge
          echo
          read -r -p "按回车关闭..."
          EOF
          chmod +x release/MantaShark-GCS-linux/start.sh release/MantaShark-GCS-linux/mavbridge
          cd release && tar -czf MantaShark-GCS-linux.tar.gz MantaShark-GCS-linux

      - uses: actions/upload-artifact@v7
        with:
          name: MantaShark-GCS-linux
          path: tools/manta_gcs/release/MantaShark-GCS-linux.tar.gz
          retention-days: 30

      - name: Attach to release
        if: github.event_name == 'release'
        uses: softprops/action-gh-release@v2
        with:
          files: tools/manta_gcs/release/MantaShark-GCS-linux.tar.gz
